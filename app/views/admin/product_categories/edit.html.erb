<h1>ジャンル編集画面</h1>

<%= form_for(@product_category, url: admin_product_category_path(@product_category)) do |f| %>

<div>ジャンル
<%= f.text_field :product_category_name %>

<input type="radio" name="有効" value="有効"> 有効
<input type="radio" name="無効" value="無効"> 無効

<%= f.submit '変更を保存する' %>
</div>

<% end %>
