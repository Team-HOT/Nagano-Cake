<p>配送先編集ページ</p>
  <% if @delivery.errors.any? %>
   <div class="alert-alert-warining">
	<p class="error-count">
		<%= @delivery.errors.full_messages.count %>errors prohibited this end_user from being saved:</p>
		<ul>
			<% @delivery.errors.full_messages.each do |message| %>
			  <li><%= message %></li>
			<% end %>
		</ul>
   </div>
  <% end %>

<%= form_for(@delivery, url: public_delivery_path) do |f| %>

<table>
	<tr>
		<th><%= f.label :郵便番号 %></th>
			<td><%= f.text_field :delivery_post %></td>
	</tr>
	<tr>
		<th><%= f.label :住所 %></th>
			<td><%= f.text_field :delivery_address %></td>
	</tr>
	<tr>
		<th><%= f.label :宛名 %></th>
			<td><%= f.text_field :delivery_name1 %></td>
	</tr>
</table>
<%= f.submit '編集する' %>
<% end %>

